<footer id="contact" class="ft" aria-label="Footer">
  <div class="ft-identity">
    <span class="ft-brand">FREDDIE K.</span>
    <span aria-hidden="true">|</span>
    <span class="ft-tagline">builder of strange tools for real life</span>
  </div>
  <nav class="ft-links" aria-label="Social links">
    <a href="https://github.com/FreddieKT" target="_blank" rel="noreferrer">GitHub</a>
    <a href="https://x.com/ktythaung" target="_blank" rel="noreferrer">X</a>
    <a href="mailto:ktythaung@gmail.com">Email</a>
  </nav>
</footer>

<style>
  .ft {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    padding: 2rem 0 1rem;
    color: var(--ink-dim);
    font-size: 0.75rem;
  }
  .ft-identity {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }
  .ft-brand { color: var(--accent); font-size: 0.6875rem; letter-spacing: 0.12em; }
  .ft-tagline { font-size: 0.75rem; color: var(--ink-faint); }
  .ft-links {
    display: flex;
    gap: 0.75rem;
    margin-left: auto;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .ft-links a { color: var(--ink-dim); padding: 0.4rem 0.2rem; }
  .ft-links a:hover { color: var(--accent); }
  @media (max-width: 720px) {
    .ft { flex-direction: column; align-items: flex-start; }
    .ft-links { margin-left: 0; }
  }
</style>
